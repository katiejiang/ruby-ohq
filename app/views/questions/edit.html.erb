<h1>Editing Question</h1>

<%= render 'form', course: @course, question: @question %>

<%= link_to 'Show', course_question_path(@course, @question) %> |
<%= link_to 'Back', course_questions_path(@course) %>
